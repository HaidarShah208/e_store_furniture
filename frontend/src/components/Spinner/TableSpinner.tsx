import Spinner from "./index";

const TableSpinner = () => {
  return (
    <div className="flex items-center justify-center w-full">
      <Spinner color={"#7973FF"} size={70} />
    </div>
  );
};

export default TableSpinner;
